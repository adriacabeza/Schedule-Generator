package model;

import java.util.HashMap;
import java.util.Map;
import java.util.Set;

public class Domini {

   /* private HashMap<Integer,HashMap <Integer, Set<String>>> domini;

    public HashMap<Integer, HashMap<Integer, Set<String>>> getDomini() {
        return domini;
    }

    public void setDomini(HashMap<Integer, HashMap<Integer, Set<String>>> domini) {
        this.domini = domini;
    }

    public Domini(HashMap<Integer, HashMap<Integer, Set<String>>> domini) {
        this.domini = domini;
    }

    public Domini aplica_restriccions_unaries(Map<String, Restriccions> restriccio){
        HashMap<Integer,HashMap <Integer, Set<String>>> Shrek;
        for(){
            Set<String> aules_valides;
            for(aules){
                if(aula.getcapacitat <= g.getcapacitat and tipus coincideix){
                    aules_valides.add(aula.getnom);
                }
            }
            //aquí ja tindria les aules vàlides segons el tipus i la capacitat

            HashMap<Integer, Set<String>> dia_hora_aula_valides;

            for(int d= 0; d <5; ++d){
                HashMap<Integer,Set<String>> hores_valides;
                for(int h = 0; h < 11; ++h){
                    Set<String> aula_valida_a_l_hora;
                    boolean valid = true;
                    for(Restriccions res: restriccio.values()){
                        if(!res.isable(dia,hora)) {
                            valid = false;
                        }
                    }
                    if(valid)

                }
            }



        }

        HashMap<Integer, Set<Aula>> hores_valides;
        HashMap<Integer,HashMap <Integer, Set<Aula>>> todo;




        return this;
    }*/
}
